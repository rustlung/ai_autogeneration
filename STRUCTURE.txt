AI Client Report Generator - Структура проекта
================================================

ai_autogeneration/
│
├── 📄 main.py                    # Точка входа, CLI интерфейс
├── ⚙️ config.py                  # Конфигурация и env переменные
├── 📋 requirements.txt           # Python зависимости
├── 🔒 .env.example              # Пример настроек (скопировать в .env)
├── 🚫 .gitignore                # Исключения для git
│
├── 📖 README.md                 # Полная документация
├── 🚀 QUICKSTART.md             # Быстрый старт за 5 минут
├── 📊 PROJECT_SUMMARY.md        # Итоги реализации
├── 📐 STRUCTURE.txt             # Этот файл
│
├── 📁 utils/                    # Утилиты и модули
│   ├── __init__.py
│   ├── schema.py                # 📋 Pydantic схемы данных
│   ├── io.py                    # 💾 Файловые операции
│   ├── logging_setup.py         # 📝 Настройка логирования
│   ├── ai_processor.py          # 🤖 OpenAI интеграция + кэш
│   └── pdf_generator.py         # 📄 HTML→PDF генерация
│
├── 📁 templates/                # Шаблоны для отчётов
│   ├── report_template.html     # 🎨 HTML шаблон отчёта
│   └── style.css                # 🎨 CSS стили для PDF
│
├── 📁 fixtures/                 # Примеры данных
│   └── sample_transcript.txt    # 💬 Демо транскрипт диалога
│
├── 📁 docs/                     # Документация
│   ├── ARCHITECTURE.md          # 🏗️ Архитектура проекта
│   └── CHANGELOG.md             # 📅 История изменений
│
├── 📁 cache/                    # Кэш (создаётся автоматически)
│   └── ai_outputs/              # 💾 Кэшированные AI ответы
│       └── .gitkeep
│
├── 📁 reports/                  # PDF отчёты (создаются автоматически)
│   └── .gitkeep
│
└── 📁 logs/                     # Логи (создаются автоматически)
    ├── .gitkeep
    └── app.log                  # 📝 Лог файл приложения


КЛЮЧЕВЫЕ ФАЙЛЫ
==============

Конфигурация:
  .env                   → Ваши настройки (создать из .env.example)
  config.py              → Загрузка и валидация конфигурации

Точка входа:
  main.py                → CLI, координация всех компонентов

Бизнес-логика:
  utils/ai_processor.py  → OpenAI API, кэширование, retry
  utils/pdf_generator.py → Jinja2 + WeasyPrint
  utils/schema.py        → Структура данных (Pydantic)

Инфраструктура:
  utils/io.py            → Чтение/запись файлов
  utils/logging_setup.py → Настройка логирования

Шаблоны:
  templates/report_template.html → HTML структура отчёта
  templates/style.css            → Оформление для PDF


CLI КОМАНДЫ
===========

Базовый запуск:
  python main.py

С параметрами:
  python main.py --input path/to/transcript.txt
  python main.py --output reports/custom_name.pdf
  python main.py --template templates/custom_template.html
  python main.py --no-cache
  python main.py --log-level DEBUG

Справка:
  python main.py --help


СХЕМА ДАННЫХ
============

ReportData (Pydantic модель):
  {
    "client_name": str,         # Имя клиента
    "topic": str,               # Тема обращения
    "main_request": str,        # Основной запрос
    "sentiment": {              # Анализ настроения
      "label": str,             #   - Метка (positive/neutral/negative)
      "score": int (1-5)        #   - Оценка от 1 до 5
    },
    "summary": str,             # Краткое содержание
    "key_points": [str, ...],   # Список ключевых моментов
    "next_steps": [str, ...]    # Рекомендуемые действия
  }


ПОТОКИ ДАННЫХ
=============

1. Чтение транскрипта
   transcript.txt → io.read_text_file() → text

2. AI обработка
   text → hash (SHA256)
        → проверка кэша
        → OpenAI API (если нет кэша)
        → JSON parsing + validation
        → ReportData (Pydantic)
        → сохранение в кэш

3. PDF генерация
   ReportData → Jinja2 template rendering → HTML
             → WeasyPrint → PDF
             → reports/report_YYYYMMDD_HHMMSS.pdf


ЗАВИСИМОСТИ
===========

Python пакеты (requirements.txt):
  - openai              → OpenAI API официальный SDK
  - jinja2              → HTML шаблонизация
  - weasyprint          → HTML→PDF конвертация
  - python-dotenv       → .env файлы
  - pydantic            → Валидация данных

Системные (для WeasyPrint):
  Windows: GTK3 Runtime
  Linux:   Cairo, Pango, GDK-PixBuf
  macOS:   brew install cairo pango gdk-pixbuf


ПЕРВЫЙ ЗАПУСК
=============

1. Установите системные зависимости (см. QUICKSTART.md)

2. Создайте виртуальное окружение:
   python -m venv venv
   venv\Scripts\activate  (Windows)
   source venv/bin/activate  (Linux/macOS)

3. Установите Python пакеты:
   pip install -r requirements.txt

4. Настройте .env:
   copy .env.example .env  (Windows)
   cp .env.example .env    (Linux/macOS)
   
   Отредактируйте .env и добавьте ваш OpenAI API ключ

5. Запустите:
   python main.py


АДАПТАЦИЯ
=========

Чтобы добавить новые поля в отчёт:

1. utils/schema.py
   → Добавить поля в класс ReportData
   → Обновить REPORT_SCHEMA_DESCRIPTION

2. utils/ai_processor.py
   → Обновить system_prompt и user_prompt
   → Указать AI что извлекать

3. templates/report_template.html
   → Добавить отображение новых полей
   → Использовать {{ report_data.новое_поле }}

4. templates/style.css (опционально)
   → Добавить стили для новых элементов


ЛОГИ
====

Местоположение: logs/app.log

Что логируется:
  - Старт/завершение приложения
  - Параметры запуска
  - Операции чтения файлов
  - Кэш hit/miss
  - OpenAI API вызовы и ответы
  - Ошибки с полным stacktrace
  - Пути к созданным PDF

Уровни:
  DEBUG   - Детальная отладка
  INFO    - Основная информация (по умолчанию)
  WARNING - Предупреждения
  ERROR   - Ошибки
  CRITICAL- Критические ошибки


БЕЗОПАСНОСТЬ
============

✅ API ключи в .env (не в коде)
✅ .env в .gitignore
✅ Валидация всех входных данных
✅ Jinja2 автоэскейпинг
✅ Атомарная запись файлов
✅ Не логируем sensitive данные


СТАТУС ПРОЕКТА
==============

🟢 ГОТОВ К ИСПОЛЬЗОВАНИЮ

Весь код реализован полностью.
Никаких placeholder'ов или "...".
Проект готов к запуску после установки зависимостей.


ПОДДЕРЖКА
=========

Документация:
  README.md         - Полное руководство
  QUICKSTART.md     - Быстрый старт
  ARCHITECTURE.md   - Детальная архитектура
  CHANGELOG.md      - История версий

При проблемах:
  1. Проверьте logs/app.log
  2. Запустите с --log-level DEBUG
  3. Проверьте установку зависимостей
  4. Убедитесь что .env настроен правильно
