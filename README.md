# AI Client Report Generator

Инструмент для быстрого создания PDF-отчётов по диалогам с клиентами. Отчёты генерируются с помощью анализа через OpenAI API и используют настраиваемые HTML-шаблоны.

## Возможности

- 🤖 **Автоматический анализ диалогов** с помощью AI (OpenAI GPT)
- 📄 **Генерация PDF-отчётов** с профессиональным оформлением
- 🎨 **Гибкие HTML/CSS шаблоны** - легко адаптировать под свои нужды
- 💾 **Кэширование AI-ответов** для экономии времени и денег
- 📝 **Детальное логирование** всех операций
- 🛡️ **Устойчивая обработка ошибок** с понятными сообщениями
- 🔄 **Retry логика** для обработки нестабильных ответов AI

## Структура проекта

```
project_root/
├── main.py                  # Точка входа в приложение
├── config.py                # Конфигурация и переменные окружения
├── requirements.txt         # Зависимости Python
├── .env.example            # Пример файла с переменными окружения
├── .env                    # Ваши настройки (создать вручную)
│
├── utils/                  # Утилиты
│   ├── ai_processor.py    # Обработка через OpenAI API
│   ├── pdf_generator.py   # Генерация PDF
│   ├── schema.py          # Pydantic схемы данных
│   ├── io.py              # Работа с файлами
│   └── logging_setup.py   # Настройка логирования
│
├── templates/              # Шаблоны для отчётов
│   ├── report_template.html
│   ├── design_report_template.html
│   └── style.css
│
├── services/               # Внешние сервисы
│   └── openai_client.py
│
├── reports/               # Готовые PDF-отчёты (создаются автоматически)
├── fixtures/              # Примеры данных
│   └── sample_transcript.txt
│   └── design_order_transcript.txt
├── cache/                 # Кэш AI-ответов
│   └── ai_outputs/
├── assets/                # Сгенерированные изображения
└── logs/                  # Логи приложения
    └── app.log
```

## Установка

### 1. Системные зависимости

**WeasyPrint** требует установки системных библиотек:

#### Windows
```bash
# Скачайте и установите GTK3 Runtime:
# https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer/releases

# После установки добавьте в PATH:
# C:\Program Files\GTK3-Runtime Win64\bin
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info
```

#### macOS
```bash
brew install cairo pango gdk-pixbuf libffi
```

### 2. Python зависимости

```bash
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Конфигурация

```bash
# Скопируйте пример файла .env
cp .env.example .env

# Отредактируйте .env и добавьте ваш OpenAI API ключ
# OPENAI_API_KEY=sk-ваш-ключ-здесь
```

Пример `.env`:
```env
OPENAI_API_KEY=sk-proj-your-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_IMAGE_SIZE=1024x1024
AI_TEMPERATURE=0
CACHE_DIR=cache/ai_outputs
LOG_LEVEL=INFO
```

## Использование

### Базовый запуск

```bash
# Запуск интерактивного меню выбора
python main.py
```

### Запуск без меню (через параметры)

```bash
python main.py --input fixtures/sample_transcript.txt --template templates/report_template.html
```

### С указанием файла

```bash
python main.py --input path/to/your/transcript.txt
```

### Управление кэшем

```bash
# Использовать кэш (по умолчанию)
python main.py --use-cache

# Принудительно запросить AI заново
python main.py --no-cache
```

### Кастомный вывод

```bash
# Указать путь к выходному PDF
python main.py --output reports/monthly_report_2026_01.pdf

# Использовать свой шаблон
python main.py --template templates/custom_template.html
```

### Тип отчёта

```bash
# Design отчёт (использует templates/design_report_template.html)
python main.py --report-type design
```

### Отладка

```bash
# Включить детальное логирование
python main.py --log-level DEBUG
```

### Полный пример

```bash
python main.py \
  --input transcripts/client_meeting_20260128.txt \
  --output reports/client_report_january.pdf \
  --template templates/report_template.html \
  --no-cache \
  --log-level INFO
```

## Адаптация под свои нужды

Проект специально разработан для лёгкой адаптации. Чтобы изменить структуру отчёта:

### 1. Обновите схему данных (`utils/schema.py`)

Добавьте новые поля в класс `ReportData`:

```python
class ReportData(BaseModel):
    client_name: str
    topic: str
    # ... существующие поля ...
    
    # Новые поля:
    budget: Optional[float] = None
    deadline: Optional[str] = None
    priority: str = "normal"
```

Не забудьте также обновить `REPORT_SCHEMA_DESCRIPTION` для AI.

### 2. Обновите AI prompt (`utils/ai_processor.py`)

Измените `system_prompt` в функции `call_openai_api()`, чтобы AI знал о новых полях:

```python
system_prompt = f"""You are an expert analyst for client conversations.
Analyze the provided dialogue transcript and extract structured information.

Extract the following:
- client_name, topic, main_request (as before)
- budget: mentioned budget or None
- deadline: mentioned deadline or None  
- priority: assess as "low", "normal", or "high"
...
"""
```

### 3. Обновите HTML-шаблон (`templates/report_template.html`)

Добавьте отображение новых полей:

```html
<section class="section">
    <h2>Бюджет и сроки</h2>
    <div class="info-grid">
        {% if report_data.budget %}
        <div class="info-item">
            <span class="info-label">Бюджет:</span>
            <span class="info-value">{{ report_data.budget }} руб.</span>
        </div>
        {% endif %}
        
        {% if report_data.deadline %}
        <div class="info-item">
            <span class="info-label">Дедлайн:</span>
            <span class="info-value">{{ report_data.deadline }}</span>
        </div>
        {% endif %}
    </div>
</section>
```

### 4. При необходимости обновите стили (`templates/style.css`)

Добавьте стили для новых элементов.

## Схема данных отчёта

AI возвращает строго типизированные данные по следующей схеме:

```json
{
  "client_name": "string",
  "topic": "string",
  "main_request": "string",
  "sentiment": {
    "label": "string (positive/neutral/negative)",
    "score": "integer 1-5"
  },
  "summary": "string",
  "key_points": ["string", "string", ...],
  "next_steps": ["string", "string", ...],
  "desired_timeline": "string or null - желаемые сроки/дедлайн",
  "budget_range": "string or null - бюджет или ожидания по стоимости",
  "core_requirements": ["string", ...] - основные требования к продукту
}
```

### Новые поля (v1.1.0+)

- **desired_timeline** - Желаемые сроки реализации (например: "через 2 месяца", "до конца квартала")
- **budget_range** - Информация о бюджете (например: "ограниченный бюджет", "облачная подписка")
- **core_requirements** - Список того, что ОБЯЗАТЕЛЬНО должно быть в финальном продукте

## Логирование

Все операции логируются в `logs/app.log` с детальной информацией:

- Старт/завершение приложения
- Параметры запуска
- Операции с кэшем (hit/miss)
- Вызовы AI API
- Ошибки с полным stacktrace
- Пути к созданным файлам

Уровни логирования:
- **DEBUG**: Детальная информация для отладки
- **INFO**: Общая информация о ходе выполнения (по умолчанию)
- **WARNING**: Предупреждения (например, невалидный кэш)
- **ERROR**: Ошибки выполнения
- **CRITICAL**: Критические ошибки

## Кэширование

Система автоматически кэширует AI-ответы на основе SHA256-хеша входного текста:

- **Первый запрос**: отправляется к OpenAI API, результат сохраняется в `cache/ai_outputs/<hash>.json`
- **Повторный запрос** с тем же текстом: берётся из кэша (быстро и бесплатно)
- **Инвалидация**: если кэш повреждён или невалиден - автоматически пересоздаётся
- **Отключение**: используйте флаг `--no-cache`

## Генерация изображений (design-отчёт)

Design-отчёт генерирует изображение через OpenAI Image API:
- prompt строится автоматически из дизайн-брифа
- кешируется в `cache/ai_outputs/image_prompt_<sha>.txt`
- изображение сохраняется в `assets/design_<sha>.png`
- при ошибке генерации отчёт всё равно создаётся (в шаблоне будет текст `image failed`)

## Обработка ошибок

Приложение устойчиво обрабатывает ошибки:

- ❌ **Файл не найден**: понятное сообщение и exit code 1
- ❌ **Ошибка кодировки**: предложение проверить UTF-8
- ❌ **Проблемы с OpenAI API**: различные типы ошибок (сеть, rate limit, timeout)
- ❌ **Невалидный JSON от AI**: автоматический retry (до 2 попыток)
- ❌ **Ошибки шаблона**: информация о синтаксических ошибках
- ❌ **Проблемы WeasyPrint**: детальная диагностика

Все ошибки логируются в `logs/app.log` с полным stacktrace.

## Exit коды

- `0` - Успешное выполнение
- `1` - Ошибка выполнения
- `130` - Прервано пользователем (Ctrl+C)

## Требования

- Python 3.8+
- OpenAI API ключ
- Системные библиотеки для WeasyPrint (см. раздел "Установка")

## Зависимости Python

- `openai` - официальный SDK для OpenAI API
- `jinja2` - шаблонизатор для HTML
- `weasyprint` - конвертация HTML в PDF
- `python-dotenv` - загрузка переменных окружения
- `pydantic` - валидация данных

## Безопасность

- ⚠️ Никогда не коммитьте файл `.env` в git
- ⚠️ Держите ваш OpenAI API ключ в секрете
- ✅ Файл `.env` уже добавлен в `.gitignore`

## Лицензия

Этот проект предоставляется как есть для использования в личных и коммерческих целях.

## Поддержка

При возникновении проблем:

1. Проверьте `logs/app.log` для детальной информации
2. Убедитесь, что все системные зависимости установлены
3. Проверьте, что OpenAI API ключ валиден и имеет доступ к выбранной модели
4. Попробуйте запустить с флагом `--log-level DEBUG` для максимальной детализации

## Примеры использования

### Пакетная обработка

```bash
# Обработать несколько файлов
for file in transcripts/*.txt; do
    python main.py --input "$file" --output "reports/$(basename "$file" .txt).pdf"
done
```

### Интеграция в CI/CD

```bash
# В вашем CI/CD скрипте
python main.py --input data/latest_transcript.txt --no-cache --log-level INFO
if [ $? -eq 0 ]; then
    echo "Report generated successfully"
else
    echo "Report generation failed"
    exit 1
fi
```

## Дополнительно

Для получения справки по всем доступным опциям:

```bash
python main.py --help
```
